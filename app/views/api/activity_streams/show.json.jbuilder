# jbuilder may be unnecessary for activity streams, as the 'types' argument
# passed into client.retrieve_activity_streams allows you to limit what
# stream data you want already.

# This array of stream objects can be directly rendered to json in the
# ActivityStreamsController
